<?xml version="1.0" encoding="UTF-8"?>
<ruleset comparisonMethod="maven"
         xmlns="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0 
                             http://mojo.codehaus.org/versions-maven-plugin/xsd/rule-2.0.0.xsd">
  <ignoreVersions>
    <!-- Ignore alpha versions -->
    <ignoreVersion type="regex">.*[-_\.](alpha|Alpha|ALPHA).*</ignoreVersion>
    <!-- Ignore beta versions -->
    <ignoreVersion type="regex">.*[-_\.](beta|Beta|BETA|b).*</ignoreVersion>
    <!-- Ignore milestone versions -->
    <ignoreVersion type="regex">.*[-_\.](milestone|Milestone|MILESTONE|m|M).*</ignoreVersion>
    <!-- Ignore release candidates -->
    <ignoreVersion type="regex">.*[-_\.](rc|RC|cr|CR).*</ignoreVersion>
    <!-- Ignore snapshot versions -->
    <ignoreVersion type="regex">.*[-_\.](snapshot|Snapshot|SNAPSHOT).*</ignoreVersion>
    <!-- Ignore preview/early access versions -->
    <ignoreVersion type="regex">.*[-_\.](preview|Preview|PREVIEW|ea|EA).*</ignoreVersion>
  </ignoreVersions>
</ruleset>
