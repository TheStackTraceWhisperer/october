<configuration>

    <!-- Set the root logging level to OFF to disable all logging -->
    <root level="OFF">
    </root>

    <!--
    The following appender and logger configurations are commented out
    because the root level is set to OFF, effectively disabling all output.
    If you need to re-enable specific logging for debugging, you can
    change the root level or uncomment and adjust these sections.
    -->

    <!--
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    -->

    <!-- Suppress Micronaut startup messages (already handled by root OFF)
    <logger name="io.micronaut.runtime.Micronaut" level="WARN"/>
    -->

    <!--
    <root level="info">
        <appender-ref ref="STDOUT" />
    </root>
    -->

</configuration>